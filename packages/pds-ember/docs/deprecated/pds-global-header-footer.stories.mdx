import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Deprecated / Global Header + Footer (Deprecated)" />

# Global Header + Footer [⚠️ deprecated ⚠️]

The components `Pds::GlobalHeader` and `Pds::GlobalFooter` are now deprecated, and not available anymore.

For more information speak with the maintainers of Cloud UI.
