<Pds::Breadcrumbs as |B|>
  {{#if @showIcon}}
    <Pds::Icon @type="folder-outline" />
  {{/if}}
  {{#each this.depths key=@index as |depth|}}
    <B.Crumb>
      <a href="#">{{depth}}</a>
    </B.Crumb>
  {{/each}}
</Pds::Breadcrumbs>
